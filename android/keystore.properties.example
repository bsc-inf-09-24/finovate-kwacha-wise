# Template for local keystore configuration.
# Copy this file to 'keystore.properties' (in this same folder) and fill in your passwords.
# The 'keystore.properties' file is git-ignored and should never be committed.

storeFile=/absolute-path
keyPassword=CHANGE_ME
